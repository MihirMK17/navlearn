[flake8]
# The CI gate formats with black and lints with flake8. E203 (whitespace before ':')
# is the one rule the two disagree on -- black emits `x[a : b]` for compound slice
# bounds and flake8 flags it -- so it is ignored here, per black's own guidance.
max-line-length = 100
extend-ignore = E203
