[flake8]
# Mirrors the repo-root .flake8 for the ament_flake8 test, which does not read the
# repository config: black emits `x[a : b]` for compound slice bounds and flake8's
# E203 flags it, so the one rule the two tools disagree on is ignored.
max-line-length = 100
extend-ignore = E203
