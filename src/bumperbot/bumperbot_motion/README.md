# bumperbot_motion

Teaching implementations of `nav2_core` controller plugins (PD motion planner, pure
pursuit) from the course lineage. Not part of any benchmark arm — the measured
controllers are Nav2's RPP/DWB/MPPI, composed by `bumperbot_navigation`.
