# bumperbot_planning

Teaching implementations of `nav2_core` global planner plugins (Dijkstra, A*) from the
course lineage. Not part of any benchmark arm — the measured planner is the vendored,
patched SMAC 2D in `third_party/nav2_smac_planner`.
