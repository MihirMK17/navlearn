# bumperbot_mapping

SLAM bringup (`slam_toolbox`) and the `small_house` occupancy map every legacy leg ran
against — course-original, never modified, validated post-hoc against the world's
collision geometry (median disagreement 0.000 m). Campaign maps for other environments
live in `navlearn_assets`.
